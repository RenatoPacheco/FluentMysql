@{
    ViewBag.Title = "Home";
}
<div class="container">
    <div class="row">
        <h2 class="text-center">@ViewBag.Title</h2>
        <div class="alertas">
            @Html.Raw(ViewBag.Mensagem)
        </div>
    </div>
</div>
